name: Keep Render alive

on:
  schedule:
    - cron: "*/5 * * * *"  # every 5 minutes (GitHub Actions minimum is 5 min)

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: curl -s -o /dev/null -w "%{http_code}" https://ai-pr-reviewer-lacj.onrender.com/docs
